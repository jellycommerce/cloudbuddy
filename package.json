{
  "name": "cloudbuds",
  "version": "1.0.0",
  "scripts": {
    "start": "cd client && npm run build && cd .. && node server --exec nodemon",
    "start-local": "npm run build-client && PORT=8080 node server"
  },
  "proxy": "http://localhost:3001",
  "dependencies": {
    "express": "^4.17.3",
    "express-pino-logger": "^4.0.0",
    "twilio": "^3.41.1"
  }
}
